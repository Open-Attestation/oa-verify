describe("OpenAttestationDnsTxt v3 document", () => {
  xit("should return a valid fragment when document has valid identity", async () => {});
  xit("should return an invalid fragment when document identity does not match", async () => {});
  xit("should return an error fragment when document has no identity type", async () => {});
  xit("should return an error fragment when document has no identity location", async () => {});

  describe("test", () => {
    xit("should return true if identityProof type is DNS-TXT", () => {});
    xit("should return false if identityProof type is not DNS-TXT", () => {});
  });
});
